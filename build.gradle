apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "Game"
version = '1.0'
mainClassName = "Game"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
